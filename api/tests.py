"""
Test module for Viaduct's api app.

Add unit and integration tests for the api here. Tests should use Django's
TestCase and DRF test utilities when exercising API endpoints.
"""

from django.test import TestCase

# Example test placeholder:
# class ApiSmokeTest(TestCase):
#     def test_root(self):
#         response = self.client.get('/api/')
#         self.assertIn(response.status_code, (200, 302))
